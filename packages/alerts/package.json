{
  "name": "@homehub/alerts",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic alert detectors. Each detector is a pure function that, given inputs pulled from app.*, returns zero or more AlertEmission objects for the alerts worker to insert into app.alert. No model calls — cost-free to re-run and idempotent via (kind, dedupeKey).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  }
}

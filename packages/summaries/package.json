{
  "name": "@homehub/summaries",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic summary renderers for the HomeHub segment digests. No model calls — renderers transform in-memory data into markdown body + a structured metrics blob. The summaries worker consumes this package and writes the output to app.summary.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}

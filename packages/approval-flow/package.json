{
  "name": "@homehub/approval-flow",
  "version": "0.0.0",
  "private": true,
  "description": "Approval state machine + policy evaluator + audit writer. Transitions suggestion/action status atomically, canonical-hashes previews for tamper detection, and writes audit.event rows for every approval / dispatch / state transition. Consumed by apps/workers/action-executor and apps/web server actions.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/db": "workspace:*",
    "@homehub/shared": "workspace:*",
    "@homehub/worker-runtime": "workspace:*",
    "@supabase/supabase-js": "^2.104.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  }
}

{
  "name": "@homehub/providers-grocery",
  "version": "0.0.0",
  "private": true,
  "description": "GroceryProvider interface + StubProvider implementation. Instacart API access is human-gated; the stub returns empty arrays so sync-grocery + the food UI can exercise the contract end-to-end before a real adapter lands. Owned by @integrations.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/worker-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}

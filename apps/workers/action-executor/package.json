{
  "name": "@homehub/worker-action-executor",
  "version": "0.0.0",
  "private": true,
  "description": "Executes approved actions via Nango and MCP, driven by the action state machine. Owned by @integrations + @memory-background; fleshed out in M9.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/approval-flow": "workspace:*",
    "@homehub/db": "workspace:*",
    "@homehub/shared": "workspace:*",
    "@homehub/worker-runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}

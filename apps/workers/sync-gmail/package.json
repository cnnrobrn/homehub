{
  "name": "@homehub/worker-sync-gmail",
  "version": "0.0.0",
  "private": true,
  "description": "Ingests labeled Gmail messages via Pub/Sub + hourly poll and writes memory events. Owned by @integrations; fleshed out in M4.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@homehub/db": "workspace:*",
    "@homehub/shared": "workspace:*",
    "@homehub/worker-runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}

{
  "name": "@homehub/hermes-router",
  "version": "0.0.0",
  "private": true,
  "description": "Always-on Railway service that routes HomeHub chat turns to per-household Hermes Agent containers. Owns Railway Management API credentials for pre-provisioning a fresh service + volume on household create, and for teardown on household delete. Proxies SSE between apps/web and the household's Hermes service.",
  "type": "module",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@homehub/db": "workspace:*",
    "@homehub/shared": "workspace:*",
    "@homehub/worker-runtime": "workspace:*",
    "e2b": "^2.19.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}

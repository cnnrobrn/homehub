/**
 * Superseded by `sandbox.ts` after the Cloud Run Sandboxes pivot.
 *
 * The old content here (Railway GraphQL mutations for long-running
 * per-household services) is preserved in git history. This stub
 * exists so we notice — via editor jump-to-def — any lingering
 * import that should point at `sandbox.ts` instead.
 */

export {};
